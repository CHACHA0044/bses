{
  "name": "@bses/supervisor",
  "version": "1.0.0",
  "private": true,
  "description": "Production process supervisor that runs all BSES backend services as isolated child processes within one Render Web Service (one public port).",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@bses/shared": "*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.7",
    "typescript": "^5.5.2"
  }
}
